
コードの修正に対し、コミットをしてください。
コミットはファイル単位でおこなってください。

次の手順で実行してください。

1. `git status` を実行する
2. ファイルを1つ選び、`git add` を実行する
3. `git diff --cached` を実行し、ファイルの更新内容を確認する
4. ファイルの更新内容からコミットメッセージを考える
5. `git commit` を実行し、コミットする
6. 選ぶファイルがなくなったら終了する

もしコミットすべきか分からないファイルがあれば、止めてください。
